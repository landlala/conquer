# 한반도 땅따먹기 게임 구현 명세 (정복 버튼 방식)

## 1. 개요

14개 팀이 한반도 지도의 땅을 차지하며 점수를 획득하는 게임이다.

기존처럼 지도에서 직접 땅을 클릭하는 방식이 아니라 **정복 버튼**을 통해 차지할 땅을 선택한다.

게임 진행 순서

1. 정복 버튼 클릭
2. 차지할 땅 선택
3. 랜덤 점수 확인
4. 점수를 받을 팀 선택
5. 최종 확인
6. 지도 및 점수판 갱신

---

## 2. 지도 구조

한반도는 총 **8개의 큰 구역**으로 구성된다.

각 큰 구역은 다시 **20개의 작은 조각**으로 구성된다.

예시

- 1-1 ~ 1-20
- 2-1 ~ 2-20
- ...
- 8-1 ~ 8-20

총 160개의 땅으로 구성된다.

큰 구역은 두꺼운 윤곽선으로 구분하며, 내부 작은 조각은 각각 독립적으로 색칠된다.

지도는 현재 점령 현황만 보여주며 직접 클릭하여 땅을 선택하지 않는다.

---

## 3. 화면 구성

### 3.1 지도 영역

- 8개의 큰 구역
- 각 구역 내부 20개의 작은 조각
- 기본 색상 또는 점령 팀 색상 표시

### 3.2 점수 순위 영역

오른쪽에는 **1조~14조**의 현재 점수를 표시한다.

표시 정보

- 현재 순위
- 팀 색상
- 조 번호
- 현재 총점

정렬 기준

1. 총점 내림차순
2. 동점이면 조 번호 오름차순

---

## 4. 정복 버튼

지도 아래에

```
[ 정복 ]
```

버튼을 배치한다.

버튼을 누르면 정복 창이 열린다.

---

## 5. 정복 창

### 5.1 땅 선택

정복 가능한 땅 목록을 표시한다.

예시

```
1-1 1-2 ... 1-20

2-1 2-2 ...

...

8-20
```

이미 점령된 땅은 비활성화한다.

### 5.2 점수 생성

땅을 선택하면 중앙에 랜덤 점수를 표시한다.

- 10점 ~ 200점
- 10점 단위
- 최초 한 번만 생성
- 다시 열어도 동일 점수 유지

```javascript
const score = (Math.floor(Math.random() * 20) + 1) * 10;
```

### 5.3 팀 선택

점수 아래에 **1조~14조** 버튼을 표시한다.

권장 배치

```
1조  2조  3조  4조  5조  6조  7조
8조  9조 10조 11조 12조 13조 14조
```

각 버튼은 고유 팀 색상을 사용한다.

---

## 6. 팀 선택 완료

최종 확인

```
3-12 땅

140점을

5조에게 부여하시겠습니까?

[취소] [확인]
```

확인을 누르면

1. 땅 소유 팀 저장
2. 팀 점수 증가
3. 지도 색상 변경
4. 순위 갱신
5. 상태 저장
6. 팝업 종료

---

## 7. 팀 정보

각 팀은 다음 정보를 가진다.

```javascript
{
    id: 1,
    name: "1조",
    color: "#xxxxxx",
    score: 0
}
```

총 **14개 팀**을 생성한다.

색상은 데이터와 UI를 분리하여 관리한다.

---

## 8. 땅 데이터

```javascript
{
    id: "3-12",
    score: null,
    ownerTeamId: null
}
```

---

## 9. 점수 생성

```javascript
function getOrCreateLandScore(land) {
    if (land.score === null) {
        land.score = (Math.floor(Math.random() * 20) + 1) * 10;
    }
    return land.score;
}
```

---

## 10. 순위 계산

```javascript
function getTeamRanking(teams) {
    return [...teams].sort((a, b) => {
        if (b.score !== a.score) {
            return b.score - a.score;
        }
        return a.id - b.id;
    });
}
```

---

## 11. 상태 저장

저장 내용

- 팀 점수
- 땅 점수
- 땅 소유 팀

```javascript
localStorage.setItem(
    "landGameState",
    JSON.stringify({
        teams,
        lands
    })
);
```

---

## 12. 게임 초기화

초기화 시

- 14개 팀 점수 초기화
- 모든 땅 소유권 제거
- 모든 땅 점수 제거
- 지도 색상 초기화
- localStorage 삭제

---

## 13. 게임 진행 흐름

```
정복 버튼 클릭
      ↓
정복 창 표시
      ↓
차지할 땅 선택
      ↓
랜덤 점수 생성
      ↓
1조~14조 중 팀 선택
      ↓
최종 확인
      ↓
지도 색상 변경
      ↓
팀 점수 증가
      ↓
순위 갱신
      ↓
상태 저장
```

---

## 14. 필수 구현 체크리스트

- [ ] 정복 버튼이 존재한다.
- [ ] 정복 창이 열린다.
- [ ] 1-1 ~ 8-20의 땅 목록이 표시된다.
- [ ] 이미 점령된 땅은 선택할 수 없다.
- [ ] 랜덤 점수는 10~200점(10점 단위)이다.
- [ ] 같은 땅은 동일한 점수를 유지한다.
- [ ] 팀 선택 버튼은 1조~14조까지 표시된다.
- [ ] 각 팀은 서로 다른 색상을 가진다.
- [ ] 확인 후 해당 땅이 팀 색으로 변경된다.
- [ ] 팀 점수가 즉시 증가한다.
- [ ] 순위표가 즉시 갱신된다.
- [ ] 새로고침 후에도 상태가 유지된다.
- [ ] 게임 초기화가 가능하다.
